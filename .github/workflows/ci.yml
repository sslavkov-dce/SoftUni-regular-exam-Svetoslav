name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run repository checks
        run: |
          echo "Running CI checks..."
          echo "No build system detected (no Node, no .NET, no Python tests)"
          echo "CI passed"